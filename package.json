{
  "name": "pglite-boilerplates",
  "version": "1.0.0",
  "description": "PGlite boilerplate templates for pgpm — scaffold in-process (WASM Postgres) workspaces and modules with no server, Docker, or services",
  "scripts": {
    "find-placeholders": "ts-node scripts/find-placeholders.ts",
    "find-placeholders:pglite": "ts-node scripts/find-placeholders.ts ./pglite",
    "update-deps": "ts-node scripts/update-deps.ts"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "ts-node": "^10.9.2",
    "typescript": "^6.0.3"
  }
}
